/** Vertical placement after fit-to-unit (blob above, terrain below). */
export const BLOB_SCENE_Y = 0.42;
/** Lower = terrain farther below the blob in the default view. */
export const TERRAIN_SCENE_Y = -0.64;
