# Debug

Use this folder to keep exported response logs for debugging.

- In the app, open the **Debug** panel and click **Download responses.json**.
- Save the file here as `emotion-planet-responses.json` (or any name) to version and inspect API responses.

The log is also stored in the browser (localStorage) and persists across reloads until you click **Clear log**.
